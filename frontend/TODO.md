# Task Progress: Fix AuthContext Error
v## Steps from Approved Plan:
- [x] 1. Delete duplicate `frontend/app/layout.tsx` (AdminLayout)
- [x] 2. Create new root layout `frontend/app/layout.tsx` (server component with AuthProvider + Navbar)
- [x] 3. Edit `frontend/app/admin/layout.tsx` (admin guard layout)
- [ ] 4. Test: Restart dev server (`npm run dev` in frontend/), check `/`, `/admin`, `/login`
- [ ] 5. Complete task
